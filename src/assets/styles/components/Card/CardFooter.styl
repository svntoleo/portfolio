.card-footer
	box-sizing border-box
	padding 1rem
	text-align center
	background white
	box-shadow inset 0 20px 20px -20px rgba(black, .15)

	.title
		font-size 1.5rem
		font-weight 900
		margin 0 0 .5rem
